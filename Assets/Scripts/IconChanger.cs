/*using Unity.VisualScripting;
using UnityEngine;
using UnityEngine.UI;
using static UnityEngine.Rendering.DebugUI;

public class IconChanger : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    [SerializeField]
    private Image[] icons = new Image[10];
    [SerializeField]
    private Sprite batteryIcon;
    [SerializeField]
    private Sprite portableBanishIcon;
    [SerializeField]
    private Sprite banishIcon;
    private void changeIcon(int itemSlotSelected, bool isOn, Item selectedItem)
    {
        switch (itemSlotSelected)
        {
            case 0:
                
                break;
            case 1:
                
                break;
            case 2:
                
                break;
            case 3:
                
                break;
            case 4:
                
                break;
            default:
                Debug.Log("Tried selecting Item out of bounds");
                break;
        }

    }
}
*/